#include <stdio.h>
#include <stdlib.h>

int main()

//Escreva um programa que leia 20 valores inteiros e escreva no final a soma dos valores positivos e a média dos valores negativos.
{
   int valor, somaPos, mediaNeg;


  while (valor <= 20)

  {
      printf("Insira um valor:");
      scanf("%d",&valor);
  }
  if (valor <= 20)
  {
      somaPos = somaPos + valor;
  }
    printf("A media dos numeros e:%d \n",somaPos);

    return 0;



    return 0;
}
